<%= form_for(@step_comment) do |f| %>
  <% if @step_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step_comment.errors.count, "error") %> prohibited this step_comment from being saved:</h2>

      <ul>
      <% @step_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :step_id %><br>
    <%= f.number_field :step_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
